import 'package:flutter/material.dart';

class TripPostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("trippost"),
      ),
      body: Text('adas'),
    );
  }
}
